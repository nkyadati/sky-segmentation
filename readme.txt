
Instructions to run the code: 

- Unzip the file, it should create a folder “sky-segmentation”
- cd “sky-segment”
- pip install -r requirements.txt
- python run.py —> Should run the FCN model on cityscape dataset and display the MAE and FScore. The results are stored in “results/results_cityscapes_fcn”

